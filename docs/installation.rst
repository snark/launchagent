============
Installation
============

At the command line::

    $ easy_install launchagent

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv launchagent
    $ pip install launchagent
